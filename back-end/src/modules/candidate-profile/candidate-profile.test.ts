import { describe, it } from "vitest";

describe("candidateProfileService", () => {
  it.todo("creates a candidate profile for a candidate user");
  it.todo("rejects profile creation when the user already has a profile");
  it.todo("deactivates the current candidate profile");
});
